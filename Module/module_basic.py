#프로그래밍의 효율을 위해 자신이 짜논 코드나 상대방의 코드를 들고와 쓸수있는기능
#모듈 들고 오기 import 모듈이름 ### 주의 포함하려는 파일이 현재 파일과 같은 디렉토리에 있어야 한다
#모듈을 다른 파일의 클래스, 함수,변수들을 가져와 사용하게 해주는것
#함수를 빌려 이용하는데 '모듈을 import햇다'라고 부른다
#만약 두 파일이 같은 경로에 있지 않다면 import할 파일의 경로를 함께 적으면된다
#모듈을 import했을시 클래스 객체로서 모듈이름.함수(~)를 써야하는데
이것을 합친것이 from 모듈이름 import모듈함수 이런식으로 쓸수있다
#모듈의 모든 함수를 포함하고 싶을때는 'from 모듈이름 import*'쓰면 된다

